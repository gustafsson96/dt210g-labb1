function Header({title}: { title: string}) {
    return (
        <>
            <header>
                <h1>{title}</h1>
                <img src="" alt="" />
            </header>
        </>
    )
}

export default Header

